import React from "react";

type Props = {
  label: string;
  event?: () => void;
  route?: string;
};

const Item: React.FC<Props> = ({ label, event, route }) => {
  return null;
};

export default Item;
